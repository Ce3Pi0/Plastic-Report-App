import { IonHeader, IonPage, IonTitle, IonToolbar } from '@ionic/react';
import './Shop.css';

const Shop: React.FC = () => {
  return (
    <IonPage>
      <IonHeader>
        <IonToolbar>
          <IonTitle>Shop</IonTitle>
        </IonToolbar>
      </IonHeader>

    </IonPage>
  );
};

export default Shop;
